# Local/machine-specific graphify state — regenerated per machine, not for git.
# mtime-based, breaks after git clone:
manifest.json
# local token-cost tracker:
cost.json
# absolute path to local interpreter:
.graphify_python
# absolute path to local checkout:
.graphify_root
# extraction cache — skip to keep repo small (safe to regenerate):
cache/
